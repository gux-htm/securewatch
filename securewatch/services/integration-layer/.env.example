# Development only — never commit real values
# Production: all values come from HashiCorp Vault (Rule S4)
NODE_ENV=development
PORT=3001
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=dev-token-only
AUDIT_HMAC_KEY=dev-hmac-key-replace-in-prod-min-32-chars
