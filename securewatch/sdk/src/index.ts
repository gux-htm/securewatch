/**
 * SecureWatch SDK — open source, published to npm.
 * Exposes event forwarding interface ONLY.
 * Zero detection logic — security requirement (steering file 02).
 *
 * Implementation: Phase 1 continuation.
 */

export type { UniversalEvent, SourceType } from '@securewatch/types';
